{
  "name": "crow",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Automatic GitHub pull request reviews using a local Codex or Claude CLI",
  "engines": { "node": ">=22.12.0" },
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "start": "node --env-file-if-exists=.env server/crow.mjs",
    "test": "node --test",
    "check": "npm test && npm run build && node --check server/crow.mjs && node --check server/github.mjs && node --check server/job-store.mjs && node --check server/review-runner.mjs"
  },
  "devDependencies": {
    "vite": "^7.1.5"
  }
}
